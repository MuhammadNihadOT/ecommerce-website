<section>
  <div class="container">
    <div class="row mt-4 mb-4">
    </div>
    <table class="table mt-4" id="zzz">
      <thead>
        <tr>
          <th scope="col">Links</th>
        </tr>
      </thead>
      <tbody>
          {{#each result}}
        <tr>
          <td>{{this.link}}</td>
            <td>{{this.description}}</td>
            <td>
            <td>
                <a style="text-decoration: none;color: aliceblue;" href="/admin/edit-link/{{this._id}}" class="btn btn-success">Edit link</a>

                <a style="text-decoration: none;color: aliceblue;" href="/admin/delete-link/{{this._id}}" onclick="return confirm('Are you want to delete {{this.name}} ')" class="btn btn-danger">Delete link</a>
            </td>
            <td>

            </td>
        </tr>
        <tr>
            
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</section>

<script>
  $(document).ready(function () {
    $('#zzz').DataTable();
  });
</script>